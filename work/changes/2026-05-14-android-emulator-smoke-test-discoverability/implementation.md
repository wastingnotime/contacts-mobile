# Implementation: Android Emulator Smoke Test Discoverability

## Summary

Updated the repository entry docs so the emulator smoke-test runbook is visible from the top-level getting-started path.

## Changes

- Added a direct pointer to `docs/operating/emulator_smoke_test_runbook.md` in `readme.md`
- Kept the pointer explicit that backend simulation comes from `../runtime-sandbox`

## Validation

- Documentation-only change
- Confirmed the top-level README now surfaces the emulator smoke-test path

